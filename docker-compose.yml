version: "3.9"

services:
  platform-upiiz-front:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: platform-upiiz-front
    ports:
      - "3030:3030"
    restart: always